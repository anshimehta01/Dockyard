{
    "configurations": [
        {
            "name": "Docker: Book Inventory Application",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "run-book-inventory-container",
            "python": {
                "pathMappings": [
                    {
                        "localRoot": "${workspaceFolder}",
                        "remoteRoot": "/app"
                    }
                ],
                "projectType": "general"
            }
        }
    ]
}